import { IPaginated } from '@common_package';
import { INotificationListOwnItemResult } from './notification-list-own.item.result';

export interface INotificationListOwnResult extends IPaginated<INotificationListOwnItemResult> {}

export interface IContactInfo {
  email: string;
}

// TODO: Remove after non-usage check
export interface IInsertContactInfo {
  email: string;
}

// TODO: Remove after non-usage check
export interface IUpdateContactInfo {
  email?: string;
}

import { CoDialogProps } from "./dialog.props"

export const dialogDefaultProps: Partial<CoDialogProps> = {
  contentProps: {
    className: "p-4"
  },
  headerProps: {
    headerTitle: {
      title: "Dialog Title"
    }
  }
}

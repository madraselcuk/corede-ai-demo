import { CoButtonProps } from "./button-props"

export const coButtonPropsDefaultValues: CoButtonProps = {
  className:
    "inline-flex items-center justify-center font-medium rounded-lg transition",
  type: "button",
  size: "default",
  variant: "default"
}

import { PasswordInputProps } from "@/components/atoms/input-password/password-input.props"

export const passwordInputTheme: Partial<PasswordInputProps> = {
  variant: "outline",
  sizeVariant: "md"
}

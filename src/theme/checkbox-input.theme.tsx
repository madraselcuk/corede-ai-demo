import { CoCheckboxInputProps } from "@/components/atoms/input-checkbox/checkbox-input.props"

export const checkboxTheme: CoCheckboxInputProps = {
  className:
    "shrink-0 cursor-pointer transition-all duration-200 focus:ring-2 focus:ring-offset-2 border-primary/30 text-primary focus:ring-primary/30 rounded border"
}
